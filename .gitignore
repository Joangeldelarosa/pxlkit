node_modules/
dist/
.next/
.turbo/
out/
build/
*.tsbuildinfo
# Env files
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local
.DS_Store
Thumbs.db
.vercel
yarn-error.log
coverage/
storybook-static/
# Test artifacts
test-results/
playwright-report/
playwright/.cache/
.artifacts/
# Local tooling
.claude/
.agents/
.planning/
.gsd/
# Internal planning / working docs (not part of the public package)
docs/superpowers/
CLAUDE.md
AGENTS.md
GEMINI.md
AUDIT.md
AUDIT-COHERENCE.md
AUDIT-FOLLOWUP.md
AUDIT-STORYBOOK.md
STORYBOOK_DEPLOY.md
# SSOT coherence audit outputs
/coherence-report.md
/coherence-report.json
/docs/coherence-snapshots/*.png
!/docs/coherence-snapshots/.gitkeep
# SSOT docs-build outputs that stay regenerable (npm run docs:build).
# Only the artifacts actually consumed by the web app are committed:
# apps/web/src/app/docs/sections/*.section.tsx (wired into /docs).
# Everything below is emitted by the pipeline but not imported anywhere yet.
README.generated.md
CHANGELOG.generated.md
/scripts/build-docs/bundle-sizes.json
/packages/ui-kit/src/registry.generated.ts
/packages/ui-kit/src/*/*.stories.tsx
/apps/web/src/app/ui-kit/sections/
/apps/web/src/app/ui-kit/*/metadata.generated.ts
# OG "images": the orchestrator currently writes the HTML template to
# public/og/*.png (Playwright screenshotting only exists in the standalone
# generate-og-images CLI) — not committable as images until that is fixed.
/apps/web/public/og/
