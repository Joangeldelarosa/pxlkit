// @ts-nocheck — generated stub. Delete this comment after curating.
/* eslint-disable */
/**
 * AUTO-GENERATED stub story for PxlKitToastProvider.
 * Source: scripts/build-docs/generate-stories.ts
 *
 * Safe to edit — once this file is committed the generator will refuse
 * to overwrite it (a hand-authored *.stories.tsx is detected).
 */

import type { Meta, StoryObj } from '@storybook/react';
import * as Component from './PxlKitToastProvider';
import manifest from './PxlKitToastProvider.manifest';
import * as examples from './PxlKitToastProvider.examples';

const meta: Meta = {
  title: 'UI Kit / Feedback / PxlKitToastProvider',
  component: (Component as any).PxlKitToastProvider ?? (Component as any).default,
  tags: ["autodocs","status-stable","cat-feedback"],
  parameters: {
    layout: 'padded',
    docs: {
      description: {
        component:
          (manifest as any)?.description ?? 'PxlKitToastProvider — generated stub.',
      },
    },
    manifest,
  },
};

export default meta;
type Story = StoryObj<typeof meta>;

/** Default */
export const Default: Story = {
  name: 'Default',
  tags: ["example-default"],
  parameters: {
    docs: { description: { story: undefined } },
  },
  render: () => {
    const ExampleComponent =
      ((examples as any).Default ?? (examples as any)['default']) ??
      ((manifest as any)?.examples?.find?.((e: any) => e?.id === 'default')?.Component);
    if (!ExampleComponent) {
      return (
        <pre style={{ color: 'crimson' }}>
          {"Missing example 'default' for PxlKitToastProvider."}
        </pre>
      );
    }
    return <ExampleComponent />;
  },
};

/** Tones */
export const Tones: Story = {
  name: 'Tones',
  tags: ["example-tones"],
  parameters: {
    docs: { description: { story: undefined } },
  },
  render: () => {
    const ExampleComponent =
      ((examples as any).Tones ?? (examples as any)['tones']) ??
      ((manifest as any)?.examples?.find?.((e: any) => e?.id === 'tones')?.Component);
    if (!ExampleComponent) {
      return (
        <pre style={{ color: 'crimson' }}>
          {"Missing example 'tones' for PxlKitToastProvider."}
        </pre>
      );
    }
    return <ExampleComponent />;
  },
};

/** Bottom Right */
export const BottomRight: Story = {
  name: 'Bottom Right',
  tags: ["example-bottom-right"],
  parameters: {
    docs: { description: { story: undefined } },
  },
  render: () => {
    const ExampleComponent =
      ((examples as any).BottomRight ?? (examples as any)['bottom-right']) ??
      ((manifest as any)?.examples?.find?.((e: any) => e?.id === 'bottom-right')?.Component);
    if (!ExampleComponent) {
      return (
        <pre style={{ color: 'crimson' }}>
          {"Missing example 'bottom-right' for PxlKitToastProvider."}
        </pre>
      );
    }
    return <ExampleComponent />;
  },
};

/** Top Center */
export const TopCenter: Story = {
  name: 'Top Center',
  tags: ["example-top-center"],
  parameters: {
    docs: { description: { story: undefined } },
  },
  render: () => {
    const ExampleComponent =
      ((examples as any).TopCenter ?? (examples as any)['top-center']) ??
      ((manifest as any)?.examples?.find?.((e: any) => e?.id === 'top-center')?.Component);
    if (!ExampleComponent) {
      return (
        <pre style={{ color: 'crimson' }}>
          {"Missing example 'top-center' for PxlKitToastProvider."}
        </pre>
      );
    }
    return <ExampleComponent />;
  },
};

/** Stacked */
export const Stacked: Story = {
  name: 'Stacked',
  tags: ["example-stacked"],
  parameters: {
    docs: { description: { story: undefined } },
  },
  render: () => {
    const ExampleComponent =
      ((examples as any).Stacked ?? (examples as any)['stacked']) ??
      ((manifest as any)?.examples?.find?.((e: any) => e?.id === 'stacked')?.Component);
    if (!ExampleComponent) {
      return (
        <pre style={{ color: 'crimson' }}>
          {"Missing example 'stacked' for PxlKitToastProvider."}
        </pre>
      );
    }
    return <ExampleComponent />;
  },
};

/** Flat */
export const Flat: Story = {
  name: 'Flat',
  tags: ["example-flat"],
  parameters: {
    docs: { description: { story: undefined } },
  },
  render: () => {
    const ExampleComponent =
      ((examples as any).Flat ?? (examples as any)['flat']) ??
      ((manifest as any)?.examples?.find?.((e: any) => e?.id === 'flat')?.Component);
    if (!ExampleComponent) {
      return (
        <pre style={{ color: 'crimson' }}>
          {"Missing example 'flat' for PxlKitToastProvider."}
        </pre>
      );
    }
    return <ExampleComponent />;
  },
};

/** Pixel Surface */
export const PixelSurface: Story = {
  name: 'Pixel Surface',
  tags: ["example-pixel-surface"],
  parameters: {
    docs: { description: { story: undefined } },
  },
  render: () => {
    const ExampleComponent =
      ((examples as any).PixelSurface ?? (examples as any)['pixel-surface']) ??
      ((manifest as any)?.examples?.find?.((e: any) => e?.id === 'pixel-surface')?.Component);
    if (!ExampleComponent) {
      return (
        <pre style={{ color: 'crimson' }}>
          {"Missing example 'pixel-surface' for PxlKitToastProvider."}
        </pre>
      );
    }
    return <ExampleComponent />;
  },
};

/** Linear Surface */
export const LinearSurface: Story = {
  name: 'Linear Surface',
  tags: ["example-linear-surface"],
  parameters: {
    docs: { description: { story: undefined } },
  },
  render: () => {
    const ExampleComponent =
      ((examples as any).LinearSurface ?? (examples as any)['linear-surface']) ??
      ((manifest as any)?.examples?.find?.((e: any) => e?.id === 'linear-surface')?.Component);
    if (!ExampleComponent) {
      return (
        <pre style={{ color: 'crimson' }}>
          {"Missing example 'linear-surface' for PxlKitToastProvider."}
        </pre>
      );
    }
    return <ExampleComponent />;
  },
};

/** Loading → Success */
export const Loading: Story = {
  name: 'Loading → Success',
  tags: ["example-loading"],
  parameters: {
    docs: { description: { story: undefined } },
  },
  render: () => {
    const ExampleComponent =
      ((examples as any).Loading ?? (examples as any)['loading']) ??
      ((manifest as any)?.examples?.find?.((e: any) => e?.id === 'loading')?.Component);
    if (!ExampleComponent) {
      return (
        <pre style={{ color: 'crimson' }}>
          {"Missing example 'loading' for PxlKitToastProvider."}
        </pre>
      );
    }
    return <ExampleComponent />;
  },
};

/** Promise Flow */
export const PromiseFlow: Story = {
  name: 'Promise Flow',
  tags: ["example-promise-flow"],
  parameters: {
    docs: { description: { story: undefined } },
  },
  render: () => {
    const ExampleComponent =
      ((examples as any).PromiseFlow ?? (examples as any)['promise-flow']) ??
      ((manifest as any)?.examples?.find?.((e: any) => e?.id === 'promise-flow')?.Component);
    if (!ExampleComponent) {
      return (
        <pre style={{ color: 'crimson' }}>
          {"Missing example 'promise-flow' for PxlKitToastProvider."}
        </pre>
      );
    }
    return <ExampleComponent />;
  },
};

/** Max Limit */
export const MaxLimit: Story = {
  name: 'Max Limit',
  tags: ["example-max-limit"],
  parameters: {
    docs: { description: { story: undefined } },
  },
  render: () => {
    const ExampleComponent =
      ((examples as any).MaxLimit ?? (examples as any)['max-limit']) ??
      ((manifest as any)?.examples?.find?.((e: any) => e?.id === 'max-limit')?.Component);
    if (!ExampleComponent) {
      return (
        <pre style={{ color: 'crimson' }}>
          {"Missing example 'max-limit' for PxlKitToastProvider."}
        </pre>
      );
    }
    return <ExampleComponent />;
  },
};

/** With Action */
export const WithAction: Story = {
  name: 'With Action',
  tags: ["example-with-action"],
  parameters: {
    docs: { description: { story: undefined } },
  },
  render: () => {
    const ExampleComponent =
      ((examples as any).WithAction ?? (examples as any)['with-action']) ??
      ((manifest as any)?.examples?.find?.((e: any) => e?.id === 'with-action')?.Component);
    if (!ExampleComponent) {
      return (
        <pre style={{ color: 'crimson' }}>
          {"Missing example 'with-action' for PxlKitToastProvider."}
        </pre>
      );
    }
    return <ExampleComponent />;
  },
};
